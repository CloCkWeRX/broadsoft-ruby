
<a href="http://www.linkedin.com/in/howethomas" >
<img src="http://www.linkedin.com/img/webpromo/btn_viewmy_160x33.gif" width="160" height="33" border="0" alt="View this profile on LinkedIn"></a>


<h2>
<%= if @leads.nil? or @leads.empty? 
      "No matching leads"
    else 
      "Matching Leads"
    end
%>
</h2> 

<% @leads.each { |e| %>
    <br>
    <%= link_to "#{e.first_name} #{e.last_name}  - #{e.title } from #{e.company}", :action => :show, :id => e.id %>
<% } unless @leads.nil? %>